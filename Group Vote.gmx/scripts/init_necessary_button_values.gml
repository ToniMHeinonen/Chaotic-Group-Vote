///init_necessary_button_values(layer, scrollable, font, hAlign);
//  If something new is added to the necessary list, this script will throw and error
//  if all the button have not added necessary arguments

layer = argument[0];
scrollable = argument[1];
font = argument[2];
hAlign = argument[3];
