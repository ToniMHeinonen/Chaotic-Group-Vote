///scr_compare_reals(first real, second real)
//argument[0] is the first number
//argument[1] is the second number

//return 1 if first greater than second
//return -1 if first less than second
//return 0 if equal
if (argument[0]) == (argument[1]) {
    return 0;
} else if (argument[0]) > (argument[1]) {
    return 1;
} else {
    return -1;
}
